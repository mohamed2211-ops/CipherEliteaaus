python3 -c "from selenium import webdriver; from selenium.webdriver.firefox.options import Options; from selenium.webdriver.firefox.service import Service; o = Options(); o.add_argument('-profile'); o.add_argument('/home/kali/whatsapp-profile'); s = Service(executable_path='/usr/local/bin/geckodriver', log_output='/dev/null'); d = webdriver.Firefox(service=s, options=o); d.get('https://web.whatsapp.com'); input('Scan QR then press Enter: '); d.quit()"
